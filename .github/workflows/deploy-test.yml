name: Deploy to Test

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: test

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Compile Java
        run: |
          mkdir -p out
          javac -d out src/*.java

      - name: Package JAR
        run: |
          cp src/question.txt out/
          cd out
          jar cfe Project3.jar Project3 *.class question.txt

      - name: Upload JAR via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.TEST_SERVER_HOST }}
          port: ${{ secrets.TEST_SERVER_PORT }}
          username: ${{ secrets.TEST_SERVER_USER }}
          password: ${{ secrets.TEST_SERVER_PASSWORD }}
          source: "out/Project3.jar"
          target: "/opt/project3/lib/"
          strip_components: 1

      - name: Restart app via SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.TEST_SERVER_HOST }}
          port: ${{ secrets.TEST_SERVER_PORT }}
          username: ${{ secrets.TEST_SERVER_USER }}
          password: ${{ secrets.TEST_SERVER_PASSWORD }}
          script: |
            pkill -f "Project3.jar" || true
            sleep 2
            nohup java -jar /opt/project3/lib/Project3.jar > /opt/project3/logs/app.log 2>&1 &
            echo "测试环境部署完成"
